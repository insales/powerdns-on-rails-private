%h1.underline
  =t :label_last_x_domains, value: @latest_domains_limit

%table
  = render partial: '/domains/domain', collection: @latest_domains

&nbsp;
%h1.underline
  = t :label_new_domain

= render partial: '/domains/new'
